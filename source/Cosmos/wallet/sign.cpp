#include <Cosmos/wallet/sign.hpp>

namespace Cosmos::nosig {

    extended_transaction transaction::sign (redeem r, keychain &k) const {
        namespace G = Gigamonkey;

        // first we use the keychain to make a redeemable transaction

        // then we use the redeemer to make the extended_transaction.

        throw 0;


    }

}
